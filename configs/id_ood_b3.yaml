# B3 — Mahalanobis Distance
# GPU job: extract_embeddings_frozen() runs a forward hook on transformer_encoder.layers.17.
# Usage:
#   JOB=B3 sbatch --gres=gpu:rtx8000:1 --cpus-per-task=4 --mem-per-gpu=16G --time=4:00:00 scripts/run_all_experiments.sh

family: id-ood-b3

analyses:
  - mahalanobis

prior_source: tabpfn_prior
device: cuda
n_synthetic: 500
n_context: 0.5
seed: 42
save: true
