# B6 — RSA (Representational Similarity Analysis)
# GPU job: same hook pattern as B4; RSA replaces CKA on the embeddings.
# Usage:
#   JOB=B6 sbatch --gres=gpu:rtx8000:1 --cpus-per-task=4 --mem-per-gpu=16G --time=4:00:00 scripts/run_all_experiments.sh

family: id-ood-b6

analyses:
  - rsa

prior_source: tabpfn_prior
device: cuda
n_synthetic: 500
n_context: 0.5
seed: 42
save: true
