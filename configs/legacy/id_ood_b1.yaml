# B1 — Shannon Entropy
# GPU job: one TabPFN forward pass per subject/EMG and per synthetic dataset.
# Usage:
#   JOB=B1 sbatch --gres=gpu:rtx8000:1 --cpus-per-task=4 --mem-per-gpu=16G --time=4:00:00 scripts/run_all_experiments.sh

family: id-ood-b1

analyses:
  - entropy

prior_source: tabpfn_prior
device: cuda
n_synthetic: 500
n_context: 0.5
seed: 42
save: true
